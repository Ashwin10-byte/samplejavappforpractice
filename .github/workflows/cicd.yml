name: End to End CI/CD workflow for Java Application with Maven, Sonar and k8s Integration

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Build with Maven
      run: mvn clean install

    - name: Perform analysis on SonarQube / SonarCloud
      uses: SonarSource/sonarcloud-github-action@v3.0.0
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TKN }}
      with:
        args: >
          -Dsonar.organization=ashwin10-byte
          -Dsonar.projectKey=Ashwin10-byte_samplejavappforpractice

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Kubernetes Context
      uses: azure/k8s-set-context@v4
      with:
        method: kubeconfig
        kubeconfig: ${{ secrets.KUBECONFIG }}
        insecure-skip-tls-verify: true

    - name: Deploy to Kubernetes
      run: |
        kubectl apply -f kubernetes/
